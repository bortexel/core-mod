CREATE TABLE IF NOT EXISTS `freezes` (`id` INTEGER PRIMARY KEY AUTOINCREMENT, `player_id` VARCHAR(100), `admin_id` VARCHAR(100), `original_speed` REAL, `freezed_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP);